---
  - name: Initial setup
    hosts: all
    roles:
      - init
  - name: Mysql setup
    hosts: db_servers
    roles: 
      - mysql
  - name: Web server setup
    hosts: web_servers
    roles:
      - agama
      - uwsgi
      - nginx
